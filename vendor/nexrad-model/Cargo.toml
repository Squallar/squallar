# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
rust-version = "1.75.0"
name = "nexrad-model"
version = "1.0.0-rc.2"
authors = ["Daniel Way <contact@danieldway.com>"]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Core data model for NEXRAD weather radar data."
readme = "README.md"
keywords = [
    "nexrad",
    "radar",
    "weather",
    "meteorology",
    "noaa",
]
categories = [
    "science",
    "parsing",
    "data-structures",
]
license = "MIT"
repository = "https://github.com/danielway/nexrad"

[features]
chrono = ["dep:chrono"]
serde = ["dep:serde"]
uom = ["dep:uom"]

[lib]
name = "nexrad_model"
path = "src/lib.rs"

[[test]]
name = "model_types"
path = "tests/model_types.rs"

[dependencies.chrono]
version = "0.4.42"
optional = true

[dependencies.hex]
version = "0.4.3"

[dependencies.serde]
version = "1.0.228"
features = ["derive"]
optional = true

[dependencies.sha2]
version = "0.10.9"

[dependencies.thiserror]
version = "2.0.16"

[dependencies.uom]
version = "0.38.0"
optional = true

# LOCAL CHANGE (squallar, see VENDORED.md). What these tables are for: this
# workspace's clippy CI job runs `cargo clippy --all-targets --all-features
# --fix` across every workspace member, **pushes the rewritten tree** to main,
# then gates on `-D warnings`. Without them a vendored crate is a standing
# invitation for a bot to rewrite upstream source, which would destroy the one
# property that makes this directory reviewable: that every line here is
# either upstream's or listed in VENDORED.md. Silencing the lints leaves the
# bot nothing to fix, and leaves the gate nothing to fail on. Same tables and
# same reasoning as the other vendored nexrad crates; the mechanism note lives
# in vendor/nexrad-decode/Cargo.toml.
#
# Representative case this was verified against: sort_by_key-shaped advice on
# the lib, which fires on the registry copy and is exactly the shape the
# fix-bot rewrites in place.
[lints.rust]
future_incompatible = { level = "allow", priority = -1 }
nonstandard_style = { level = "allow", priority = -1 }
rust_2018_idioms = { level = "allow", priority = -1 }
unused = { level = "allow", priority = -1 }
deprecated = "allow"
unexpected_cfgs = "allow"
unknown_lints = "allow"
renamed_and_removed_lints = "allow"

[lints.clippy]
all = { level = "allow", priority = -1 }

