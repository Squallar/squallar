//! Raster data types for Level III products (Phase 2 stub).

/// A decoded raster data packet (stub for future grid-based products like VIL, ET).
#[derive(Debug, Clone)]
pub struct RasterPacket {
    /// Placeholder — will contain the decoded raster grid.
    _private: (),
}
