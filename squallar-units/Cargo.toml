[package]
name = "squallar-units"
version.workspace = true
edition.workspace = true

[dependencies]
chrono = { workspace = true }
serde = { workspace = true }

[dev-dependencies]
# `DataSize`'s round-trip test only; the crate itself emits no JSON.
serde_json = { workspace = true }
